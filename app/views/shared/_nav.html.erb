<nav class="flex justify-center space-x-4 bg-blue-500 px-2">
  <div class="container mx-4 flex flex-wrap items-center justify-between">
    <%= link_to fa_icon("chevron-circle-left"), root_path, class: "text-slate-100" %>
    <a href="#" class="flex">
      <span class="self-center text-lg font-semibold whitespace-nowrap text-slate-100">Budget App</span>
    </a>
    <button data-collapse-toggle="mobile-menu" type="button" class="md:hidden ml-3 text-gray-400 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-300 rounded-lg inline-flex items-center justify-center" aria-controls="mobile-menu-2" aria-expanded="false">
      <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="#fff" class="bi bi-list" viewBox="0 0 16 16">
        <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
      </svg>
    </button>
    <div class="hidden justify-between items-center w-full md:flex md:w-auto md:order-1 lg:flex" id="mobile-menu">
      <% if user_signed_in? %>
        <ul class="flex">
          <li class="text-slate-100">Logged in as <strong><%= current_user.email %></strong>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class:"font-medium px-3 py-2 text-slate-100 border-1 rounded-lg hover:bg-slate-100 hover:text-slate-900" %>
          </li>
        <% else %>
          <li><%= link_to "Sign up", new_user_registration_path, class:"font-medium px-3 py-2 text-slate-100 border-1 rounded-lg hover:bg-slate-100 hover:text-slate-900"%>  |
            <%= link_to "Sign in", new_user_session_path, class:"font-medium px-3 py-2 text-slate-100 border-1 rounded-lg hover:bg-slate-100 hover:text-slate-900" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
