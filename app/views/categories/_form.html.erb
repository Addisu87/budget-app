<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, class: "relative" %>
    <%= form.text_field :name, autocomplete: "name",placeholder: "Name", class: "border-slate-400 w-full", required: true %>
  </div>
  <div>
    <%= form.label :icon, style: "display: inline" %>
    <%= form.text_field :icon, autocomplete: 'icon', placeholder: "Icon", class: "mt-3 border-slate-400 w-full", required: true %>
  </div>
  <div class="mt-3">
    <%= form.submit class: "btn-primary" %>
  </div>
<% end %>
